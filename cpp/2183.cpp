#include <bits/stdc++.h> 

using namespace std; 

int main(){
    int n, ans,a ;
    vector<int> coins;
    for (int i = 0; i < n; i++){ 
        cin >> a;
        coins.push_back(a); 

    }
    sort(coins.begin(),coins.end());
    

}